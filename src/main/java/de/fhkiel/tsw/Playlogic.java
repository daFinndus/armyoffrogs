package de.fhkiel.tsw;

import de.fhkiel.tsw.armyoffrogs.Position;

/**
 * Die Playlogic Klasse enthält die Spiellogik für das Bewegen von Fröschen.
 * Sie bietet eine Methode an, um zu überprüfen, ob ein Zug erlaubt ist.
 */

public class Playlogic {
  /**
   * Check if the move is allowed.
   *
   * @param frog     The frog that is moved
   * @param position The position the frog is moved to
   * @return true if the move is allowed, false otherwise
   */
  public boolean isMoveAllowed(Frog frog, Position position) {
    // Implement the logic to check if the move is allowed
    return true;
    // Return true if the move is allowed, false otherwise
  }
}
